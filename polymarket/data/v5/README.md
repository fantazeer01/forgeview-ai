# Edge Engine v5 Data

Use this directory for durable v5 fixtures or externally supplied evidence
inputs. Runtime captures are stored under `polymarket/runs/v5/`.

The deterministic replay source is the timestamped capture's `session.jsonl`.
