# Edge Engine v4 Data

Place durable v4 input fixtures or exported scanner sessions here.

Runtime captures and reports belong under `polymarket/runs/v4/`. A complete
session consists of `markets.json`, `reference_prices.jsonl`, and
`polymarket_snapshots.jsonl`.
